

export default function Footer() {
    return (
        <footer className="mt-5 p-4 text-center border-t border-gray-300 w-full">
            <p>{new Date().getFullYear()} Mi portafolio</p>
        </footer>
    );
}
